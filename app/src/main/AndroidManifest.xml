<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rentalapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TotalHousesButtons"></activity>
        <activity android:name=".PostAd" />
        <activity android:name=".UserType" />
        <activity android:name=".DisplayHouses" />
        <activity android:name=".PaymentScreen"/>
        <activity android:name=".CreateAdScreen" />
        <activity android:name=".DisplayRooms" />
        <activity android:name=".DisplayeApartment" />
        <activity android:name=".SelectionScreen" />
        <activity android:name=".NewUserScreen" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>